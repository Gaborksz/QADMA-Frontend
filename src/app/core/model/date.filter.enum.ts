export enum DateFilterSelectorType {
    NONE = 'NONE',
    ON = 'ON',
    BEFORE = 'BEFORE',
    AFTER = 'AFTER',
    BETWEEN = 'BETWEEN'
} 

export const DATE_FILTER_SELECTOR_LIST = Object.values(DateFilterSelectorType);