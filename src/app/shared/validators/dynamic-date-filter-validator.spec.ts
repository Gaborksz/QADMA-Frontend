import { DynamicDateFilterValidator } from './dynamic-date-filter-validator';

describe('DynamicDateFilterValidator', () => {
  it('should create an instance', () => {
    expect(new DynamicDateFilterValidator()).toBeTruthy();
  });
});
